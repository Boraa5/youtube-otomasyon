name: YouTube AI Studio Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "================================"
          echo "YOUTUBE AI STUDIO TEST BASLADI"
          echo "================================"
          python --version
          echo "GitHub Actions CALISIYOR"
