name: YouTube Otomasyon Sistemi

on:
  workflow_dispatch:
  schedule:
   - cron: '0 */6 * * *'"
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Sistem çalışıyor
        run: |
          echo "YouTube otomasyon sistemi çalışıyor"
          run: |
